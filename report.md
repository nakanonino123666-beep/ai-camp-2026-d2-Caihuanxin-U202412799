# D2 报告：混凝土裂缝图像筛查器

## 1. 本日问题

- 里程碑：day-02
- 学生或小组：蔡焕新
- 使用者：需要先筛选大量照片、再安排人工复核的设施维护团队
- 真实输入：Kaggle Concrete Crack Images——Positive（有裂缝）与 Negative（无裂缝）各 20,000 张真实混凝土表面照片
- 需要的输出：一个可运行的初筛程序：读取真实图像，比较多数类基线与 SmallCNN，列出错误图像，并明确展示漏检风险
- 与使用者最相关的错误：漏检裂缝（假阴性）——真实有裂缝却被预测为无裂缝，导致需要人工查看的照片没有被优先发现
- 本日产品边界：输出只用于安排人工复核顺序，不能替代现场检查、工程师判断或安全决策

## 2. 真实数据或真实课程输入

- 所有者/发布者：Kaggle 数据集 `arunrk7`
- 标题：Surface Crack Detection
- 原始 URL：https://www.kaggle.com/datasets/arunrk7/surface-crack-detection
- 许可标签：以 Kaggle 页面标注为准
- 下载/取得日期：2026-08-18
- 预期文件与结构：解压后 `Positive/` 与 `Negative/` 两个文件夹，各 20,000 张图像（.jpg/.jpeg/.png），放入 `data/raw/`
- 检查命令：`python train.py --check-data`
- 实际检查结果（真实输出）：

```text
REAL DATA CHECK PASSED
class_root: data\raw
counts: {'Negative': 20000, 'Positive': 20000}
```

- 已知缺失、偏差或限制：检查器只验证文件夹结构与数量，不能证明数据无偏差、许可适合所有用途；训练只用固定子集（每类 600 张），不代表全部 40,000 张的分布；随机拆分会把高度相似的图像块分到训练/测试两边，存在相关图像泄漏风险（本 starter 使用按类别随机抽样，未按表面分组，结论不能推广到全部 40,000 张）。

## 3. 可复现运行

```powershell
# 当前目录
cd student-work\day-02-concrete

# 安装
python -m pip install -r requirements.txt

# 数据检查
python train.py --check-data

# 主程序（基线）
$env:KMP_DUPLICATE_LIB_OK="TRUE"
python train.py --model baseline

# 主程序（候选 CNN）
python train.py --model cnn

# 错误分析（同一划分，全部错误）
python analyze_errors.py

# 测试
python -m unittest discover -s tests -v
```

本机已实际执行并记录的输出：

- 环境：Python 3.13.5、torch 2.13.0+cpu、torchvision 0.28.0+cpu、matplotlib 3.10.7
- 测试：3/3 通过（`OK`）——`test_cnn_output_has_two_class_scores`、`test_balanced_split_uses_both_classes`、`test_false_negative_count`
- 张量形状追踪（真实输出）：输入 `(4, 3, 64, 64)` → Conv2d `(4, 8, 64, 64)` → ReLU `(4, 8, 64, 64)` → MaxPool2d `(4, 8, 32, 32)` → Conv2d `(4, 16, 32, 32)` → ReLU `(4, 16, 32, 32)` → MaxPool2d `(4, 16, 16, 16)` → Flatten `(4, 4096)` → Linear `(4, 2)`；总参数量 9,586
- 主程序输出文件：`runs/baseline.json`、`runs/cnn.json`、`runs/baseline-errors.png`、`runs/cnn-errors.png`、`runs/error-analysis.csv`
- 可复现性旁证：`analyze_errors.py` 用同一 seed、同一划分独立重训，得到与 `cnn.json` 完全一致的 43 个漏检 + 13 个误报

## 4. 基线与候选

### 简单基线

- 方法：多数类基线（train.py 的 `majority_baseline`）——总是预测训练子集中数量最多的类别，完全不读取图像像素
- 为什么足够简单：它不需要任何学习，只用一次类别计数；如果 CNN 不能明显超过"总猜一种类别"，说明复杂方法没有带来可解释的收益
- 命令：`python train.py --model baseline`
- 结果（真实输出）：训练集两类各 450 张**完全均衡**，"多数类"平票时基线退化为一律预测 crack——准确率 0.5、裂缝召回率 1.0、误报 150 张。它"不漏裂缝"，但把一半照片全标成裂缝，等于没有筛选作用。

### 候选方法

- 学生完成的核心改动：`models.py` 中 `SmallCNN` 的两个 TODO——按课程给定结构用 `nn.Sequential` 搭建两层卷积网络，并让 `forward` 返回网络输出（见第 3 节形状追踪）
- 保持不变的数据、划分、指标或参数：数据来源、`--max-per-class 600`、75/25 划分、`--seed 2026`、Adam lr=0.001、CrossEntropyLoss、`--epochs 2`、评估函数与指标定义全部未改
- 命令：`python train.py --model cnn`
- 结果（真实输出）：训练损失 [0.6818, 0.6186]，CPU 约 11 秒

| 项目 | 基线 | 候选 | 含义 |
| --- | ---: | ---: | --- |
| 准确率 | 0.500 | 0.8133 | 预测正确样本占比（244/300） |
| 裂缝召回率 | 1.000 | 0.7133 | 真实裂缝中被发现的比例（107/150）；基线的 1.0 来自"全猜 crack"，误报 150 张，没有筛选价值 |
| 裂缝精确率 | 0.500 | 0.8917 | 被标"有裂缝"的图里真正有裂缝的比例（107/120） |
| 漏检裂缝数（假阴性） | 0 | 43 | 与使用者最相关的错误：需要人工查看却没被优先发现 |
| 混淆矩阵 [no_crack, crack] | [[0,150],[0,150]] | [[137,13],[43,107]] | 行=真实，列=预测 |

我用混淆矩阵亲手验算了两个指标，确认对应关系没有填错：

- 准确率 = (137 + 107) / 300 = 244 / 300 ≈ 0.8133；
- 裂缝召回率 = TP / (TP + FN) = 107 / (107 + 43) = 107 / 150 ≈ 0.7133——150 张真裂缝里有 107 张被发现；
- 裂缝精确率 = TP / (TP + FP) = 107 / (107 + 13) = 107 / 120 ≈ 0.8917——被标"有裂缝"的 120 张里有 107 张是真的。

结论：CNN 明显比基线有用——它把误报从 150 张降到 13 张（筛查效率大幅提高），同时仍能找出 107/150 张真裂缝。但它**漏检了 43 张真裂缝**：这些照片需要人工查看却不会被优先发现，这正是初筛工具必须向使用者明示的风险。

## 5. 一个真实失败案例

从 `runs/cnn.json` 的 `first_errors` 和 `runs/error-analysis.csv` 中选了一个真实的**漏检裂缝**（假阴性）——这是本场景里最该向使用者解释的错误类型：

```text
原始样本位置：data\raw\Positive\09570.jpg（Positive 文件夹 = 真实有裂缝）
真实结果：crack（有裂缝）
系统输出：no_crack（无裂缝，漏检）
程序测得的图像统计：亮度 0.67、对比度 0.08、边缘密度 0.0140
对比：正确识别的裂缝组（107 张）均值 亮度 0.607、对比度 0.120、边缘密度 0.0198
这个案例说明的模型限制：这张图比"平均裂缝图"更亮、对比更弱、裂缝边缘信号更不明显，
                     模型把偏弱的外观信号当成了干净表面
这个案例不能证明什么：不能证明亮度与漏检之间有因果关系，也不能代表模型对
                     所有类似外观的图都会漏检——它只说明这个样本上的局限
下一项检查：人工打开 09570.jpg 确认裂缝形态；统计全部 43 张漏检图的亮度分布，
           与 107 张正确识别的裂缝图对比（组均值已算出：0.716 vs 0.607）
```

全部 56 个错误的分组统计（`runs/error-analysis.csv`，程序测得，非目测）：

| 组 | 数量 | 平均亮度 | 平均对比度 | 平均边缘密度 |
| --- | ---: | ---: | ---: | ---: |
| TP 正确识别裂缝 | 107 | 0.607 | 0.120 | 0.0198 |
| FN 漏检裂缝 | 43 | 0.716 | 0.113 | 0.0166 |
| TN 正确识别无裂缝 | 137 | 0.734 | 0.035 | 0.0117 |
| FP 误报 | 13 | 0.548 | 0.034 | 0.0150 |

两组观察：漏检的裂缝图整体更亮（0.716 vs 0.607）、边缘信号更弱（0.0166 vs 0.0198）；误报的无裂缝图是四组里最暗的（0.548），但边缘密度高于正常无裂缝图（0.0150 vs 0.0117）——暗表面上的阴影或污渍边缘容易被当成裂缝。注意这只是固定子集上的组均值观察，不是因果结论。

## 6. 智能体与学生工作边界

- 智能体提出/生成/修改了什么：复制 starter 到 `student-work/day-02-concrete`；完成 `models.py` 的 SmallCNN（TODO 1、TODO 2）；安装 CPU 版 torch/torchvision 依赖；运行单元测试、形状追踪与梯度检查；数据到位后运行 `--check-data`、基线与 CNN 训练并保存输出；编写 `analyze_errors.py` 做全部错误的分组统计；起草 README、report、submission.json 与 presentation.pptx
- 学生怎样核对文件、来源、输出、测试和 diff：运行 `git diff` 确认 `train.py`、测试与指标定义未被修改；核对测试 3/3 通过；对照第 3 节形状追踪逐层确认输出维度；亲手把 `runs/cnn.json` 的数字与报告表格逐一核对（召回率 = 107/150、精确率 = 107/120 的验算过程见第 4 节）；打开 `runs/cnn-errors.png` 人工查看错误图像
- 学生修改或拒绝了什么建议：如实填写。已知的一次修正：`analyze_errors.py` 第一版把真实标签按列表顺序而非按索引对齐，导致错误计数变成 120 个误报——检查后定位到 `zip(test_indices, targets, predictions)` 没有按索引取标签，改成 `targets[index]` 后与 `cnn.json` 的 43+13 完全一致
- 每名成员能独立解释的代码或证据：SmallCNN 每层的作用与形状变化；基线为什么只需要一次类别计数、以及数据均衡时"多数类"平票会发生什么；裂缝召回率怎么从混淆矩阵算出来；一条图像从 `ImageFolder` 读取 → resize 64×64 → 张量 → 模型 → argmax → 混淆矩阵的完整路线

## 7. 结论与限制

在每类 600 张的固定子集（300 张留出测试图，seed 2026）上，SmallCNN 的准确率 0.813 明显超过多数类基线的 0.5，且把误报从 150 张降到 13 张——候选方法在同一条件下确实比基线更有用。但 CNN 漏检了 43/150 张真裂缝：筛查工具"优先推送可能有裂缝的照片"的同时，会把这些漏检照片排在后面，所以输出只能用于安排人工复核顺序，任何情况下都不能当作"无裂缝"的安全结论。

数据限制：子集按类别随机抽样，未按混凝土表面分组，来自同一表面的相似图像块可能同时出现在训练和测试两边，测试分数可能过于乐观；子集结果不代表全部 40,000 张。方法限制：SmallCNN 只有 9,586 个参数、只训练 2 个 epoch，结论只证明"这个结构比总猜一类更有用"，不证明接近最优。决策限制：模型没有不确定性估计，异常材质、不同拍摄环境的照片必须交给人；筛查结果不能替代现场检查、工程师判断或安全决策。

## 8. 提交复核

- [x] README 从新环境可以开始运行（含 OpenMP 环境变量说明与真实预期输出）
- [x] 数据检查、测试和主程序重新运行（`REAL DATA CHECK PASSED`、3/3 测试、基线+CNN 输出已保存）
- [x] 报告数字与保存输出一致（第 4 节数字均出自 `runs/*.json`，并已用混淆矩阵验算）
- [x] `presentation.pptx` 在 3 分钟内讲完（4 页，已填入真实数字）
- [x] `submission.json` 路径正确
- [x] 无密钥、大数据、私人信息、虚拟环境或缓存（`data/raw/` 与 `runs/` 被 `.gitignore` 排除）
- [ ] GitHub 网页复查并邮件发送 URL（由学生本人完成）
